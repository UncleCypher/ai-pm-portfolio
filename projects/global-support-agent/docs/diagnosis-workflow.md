# 诊断流程

MVP 流程：

1. 接收用户问题、国家、语言和设备信息。
2. 安全规则优先扫描危险信号。
3. 危险场景创建模拟人工工单并停止自动诊断。
4. 普通场景识别标准症状。
5. 从内置版本化知识中检索候选根因。
6. 返回排序后的根因、引用和一项低风险检查。
7. 未识别问题请求错误码和发生步骤。

后续需要增加诊断会话持久化和步骤结果回传，使同一 `session_id` 能够更新候选根因并继续下一步。

